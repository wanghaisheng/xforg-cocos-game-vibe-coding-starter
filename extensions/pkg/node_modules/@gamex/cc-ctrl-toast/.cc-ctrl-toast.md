# toast控件

## 前置条件
无

## 使用案例
将Toast.prefab拖至业务UI中
```TS
import {} from 'db://pkg/@gamex/cc-ctrl-toast';

// 添加一个提示
node.getComponent(Toast).add({title: '提示框'});

// 清空所有
node.getComponent(Toast).clear();
```
